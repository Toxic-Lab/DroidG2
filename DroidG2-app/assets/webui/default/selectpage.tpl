#if $pgname = home
	#include home.tpl
#endif 

#if $pgname = dl
	#include downloads.tpl
#endif 

#if $pgname = dlinfo
	#include dlinfo.tpl
#endif 

#if $pgname = upl
	#include uploads.tpl
#endif 

#if $pgname = srch
	#include searches.tpl
#endif 

#if $pgname = srchinfo
	#include results.tpl
#endif 

#if $pgname = incoming
	#include incoming.tpl
#endif 

#if $pgname = settings
	#include settings.tpl
#endif 

#if $pgname = log
	#include log.tpl
#endif 

#if $pgname = share
	#include share.tpl
#endif 

